name: zero-shot-phobert-base

data:
  data_path: data/processed
  corpus_from: eval

retrieval:
  method: dense
  top_k: 100
  params:
    model_name: vinai/phobert-base
    segmentation: pyvi
    encode_kwargs: { batch_size: 64 }
    max_length: 256

reranking: null

evaluation:
  metrics:
    all

output:
  predictions: results/model-selection/phobert-base/outputeval.txt
  scores: results/model-selection/phobert-base/scores.json
