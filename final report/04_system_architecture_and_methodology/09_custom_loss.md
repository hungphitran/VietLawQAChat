# Custom Loss

The current training code uses standard ranking losses for the bi-encoder and label-based training for the cross-encoder. If a custom loss is part of the final project scope, this section should explain the motivation clearly: for example, to emphasize hard negatives, improve margin control, or better reflect the structure of legal retrieval.

If the custom loss is not yet finalized, the report should not present it as a finished contribution. Instead, it should be described as an experimental direction within the current system, with the baseline loss used as the stable reference point.
