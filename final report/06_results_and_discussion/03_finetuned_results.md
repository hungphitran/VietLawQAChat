# Fine-tuned Retrieval Results

Fine-tuned retrieval results should be reported only when the training runs are stable and the checkpoints are ready for evaluation. If the final trained bi-encoder and cross-encoder are not yet complete, this subsection should be kept as a placeholder for the finished report rather than filled with speculative claims.

What can already be stated is that the project has the correct training structure in place. Once the final runs are done, this section should focus on the delta between zero-shot retrieval and trained retrieval, because that is the clearest way to show the value of the training pipeline.
